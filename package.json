{
  "devDependencies": {
    "husky": "^7.0.2",
    "lint-staged": "^11.1.2",
    "prettier": "^2.0.5"
  },
  "lint-staged": {
    "*.{js,css,md,json,yaml,ts}": "prettier --write"
  },
  "scripts": {
    "postinstall": "husky install",
    "prepare": "husky install"
  }
}
